import { css } from '@emotion/react';

export default css`
  body {
    margin: 0;
    color: gray;
  }
  input {
    outline: none;
    border: none;
    box-sizing: border-box;
  }
  textarea {
    outline: none;
    border: none;
    box-sizing: border-box;
    resize: none;
  }
  a {
    color: inherit;
    text-decoration: none;
  }
  button {
    outline: none;
    border: none;
    padding: 0;
    background: none;
    cursor: pointer;
  }
`;
